import React from 'react';
import { storiesOf } from '@storybook/react';
import SelectListButton from './SelectListButton';

storiesOf('Select List', module).add('Select List', () => <SelectListButton />);
